# student-struct
# Student Struct

A simple Rust program defining a `Student` struct with `name` and `grade` fields, plus a method to print a summary.

## Running it

```bash
cargo run
```

## Example output

```
Milah is in grade 10.
```
